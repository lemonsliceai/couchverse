# LiveKit — realtime audio/video
LIVEKIT_URL=ws://localhost:7880
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# Groq — STT (Whisper) and LLM (Llama Scout)
GROQ_API_KEY=

# ElevenLabs — TTS
ELEVEN_API_KEY=

# LemonSlice — avatar rendering
LEMONSLICE_API_KEY=

# PostgreSQL (Neon or local)
DATABASE_URL=

# LiveKit agent name — must match between API and agent worker.
# Use a unique name for local dev so dispatches don't collide
# with the deployed worker.
AGENT_NAME=podcast-commentary-agent-local

# FoxConfig preset — selects which personality / tuning file to load from
# src/podcast_commentary/agent/fox_configs/<FOX_CONFIG>.py.
# Only consulted when PERSONAS is empty; `fox` is the stock preset.
FOX_CONFIG=fox
